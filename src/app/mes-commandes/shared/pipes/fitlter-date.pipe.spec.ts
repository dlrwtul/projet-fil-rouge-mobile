import { FitlterDatePipe } from './fitlter-date.pipe';

describe('FitlterDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FitlterDatePipe();
    expect(pipe).toBeTruthy();
  });
});
