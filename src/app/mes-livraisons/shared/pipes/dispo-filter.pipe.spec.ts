import { DispoFilterPipe } from './dispo-filter.pipe';

describe('DispoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DispoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
